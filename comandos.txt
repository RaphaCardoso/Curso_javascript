pwd => comando que verifica o caminho que vc está

ls -la =>

ctrl + l => limpa

 entrar na pasta => git clone
https://github.com/RaphaCardoso/Curso_javascript.git

git status

git add . 

git commit -m "{algo}"

git push origin main 

git pull => para pegar o repositório atualizado

git checkout -b "nome branch" para criar uma branch

git branch --all para ver as branch que existem